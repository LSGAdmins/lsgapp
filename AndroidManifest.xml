<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lsg.app"
    android:versionCode="1"
    android:versionName="@string/versioncode" android:installLocation="preferExternal">

    <uses-sdk android:minSdkVersion="7" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".lsgapp"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/vplan" android:name="VPlan"></activity>
        <activity android:label="@string/events" android:name="Events"></activity>
        <activity android:name="Settings" android:label="@string/settings"></activity>
        <activity android:name="HelpAbout"></activity>
        <activity android:label="@string/smvblog" android:name="SMVBlog"></activity>
        <activity android:label="@string/updatecheck" android:name="UpdateCheck"></activity>
    </application>

</manifest>